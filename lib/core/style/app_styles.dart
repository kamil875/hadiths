import 'package:flutter/material.dart';

class AppStyles {
  static const mainPadding = EdgeInsets.all(16);
  static const mainPaddingMini = EdgeInsets.all(8);

  static const mainPaddingHorizontal = EdgeInsets.symmetric(horizontal: 16);
  static const mainPaddingHorizontalMini = EdgeInsets.symmetric(horizontal: 8);

  static const mainMargin = EdgeInsets.all(16);
  static const mainMarginMini = EdgeInsets.all(8);
  static const mainMarginLeft = EdgeInsets.only(left: 16);

  static const mainBorder = BorderRadius.all(Radius.circular(20));

  static const List<double> textSizes = [
    18.0,
    22.0,
    30.0,
  ];
}